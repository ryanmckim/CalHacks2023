import React, { useState, useEffect } from 'react';

export default function App() {
  // const [data, setData] = useState([]);

  // useEffect(() => {
  //   fetch("/members")
  //     .then(res => res.json())
  //     .then(data => {
  //       setData(data);
  //     });
  // }, []);

  return (
    <div>
      {/* {data.length === 0 ? (
        <p>Loading...</p>
      ) : (
        data.map((member, i) => (
          <p key={i}>{member}</p>
        ))
      )} */}
    </div>
  );
}
